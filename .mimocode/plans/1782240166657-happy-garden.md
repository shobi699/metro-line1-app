# برنامه تکمیل پروژه — باقیمانده‌ها و اصلاحات

**پروژه**: metro-line1-app  
**تاریخ**: ۲۰۲۶-۰۶-۲۳  
**وضعیت فعلی**: ۲۴ فاز توسعه بصری انجام شده. build عبور می‌کند. lint دارای 32 خطای pre-existing است.

---

## خلاصه وضعیت

| وضعیت | تعداد |
|-------|-------|
| ✅ انجام شده | ۱۴ مورد |
| ⚠️ نیمه‌کاره | ۶ مورد |
| ❌ باقیمانده | ۱۶ مورد |

---

## بخش ۱: اصلاحات فوری (خطاها و مشکلات ساختاری)

### ۱.۱ رفع 32 خطای lint
- **فایل‌ها**: `src/app/api/admin/roles/`, `src/app/api/admin/users/`, `src/app/admin/users/page.tsx`, `src/stores/auth.ts`, `src/server/modules/roster/service.ts`
- **تغییرات**: جایگزینی `any` با тип‌های صحیح، حذف `require()` imports، اصلاح `setState` در effect
- **اولویت**: بالا

### ۱.۲ جایگزینی 39 رنگ hardcoded باقیمانده
- **فایل‌ها**: `admin/exams-editor/`, `admin/operator-licenses/`, `admin/license-plates/`, `admin/infrastructure/`, `admin/biometrics/`, `admin/documents-queue/`
- **تغییرات**: `emerald-*` → `success`, `red-*` → `critical`
- **اولویت**: متوسط

### ۱.۳ حذف 2 `console.log` از API routes
- **فایل‌ها**: `api/auth/otp/send/route.ts`, `api/debug/route.ts`
- **تغییرات**: حذف یا جایگزینی با silent catch
- **اولویت**: بالا

---

## بخش ۲: اصلاحات ساختاری معماری

### ۲.۱ ایجاد `src/features/` و انتقال storeها
- **تغییرات**: ایجاد `src/features/{auth,directory,roster,swap,safety,tickets}/` و انتقال storeها از `src/stores/`
- **فایل‌ها**: 7 فایل store از `src/stores/` به `src/features/*/store.ts`
- **اولویت**: متوسط

### ۲.۲ ایجاد `src/lib/zod/` و انتقال schemas
- **تغییرات**: انتقال Zod schemas از `src/server/dto/` به `src/lib/zod/` و ایجاد barrel exports
- **اولویت**: متوسط

### ۲.۳ اضافه کردن `persist` middleware به Zustand stores
- **تغییرات**: اضافه کردن `persist` middleware به هر 7 store
- **اولویت**: پایین

---

## بخش ۳: اصلاحات RTL

### ۳.۱ جایگزینی 97 کلاس `left`/`right` با ویژگی‌های منطقی
- **تغییرات**: `right-3` → `end-3`, `left-0` → `start-0`, `text-right` → `text-end`
- **فایل‌ها**: 21 فایل در `src/app/` و `src/components/`
- **اولویت**: متوسط

### ۳.۲ حذف 21 `style={}` inline
- **تغییرات**: جایگزینی با کلاس‌های Tailwind یا متغیرهای CSS
- **اولویت**: پایین

---

## بخش ۴: کامپوننت‌های UI_missing

### ۴.۱ نصب shadcn components_missing
- **لیست**: `dropdown-menu`, `tooltip`, `toast/sonner`, `separator`, `scroll-area`, `tabs`, `avatar`, `checkbox`, `switch`, `popover`, `sheet`, `skeleton`, `form`
- **تغییرات**: `npx shadcn add <component>` برای هر کدام
- **اولویت**: متوسط

### ۴.۲ ایجاد `src/components/shared/AppShell`
- **تغییرات**: استخراج AppShell از `app/(app)/layout.tsx`
- **اولویت**: پایین

---

## بخش ۵: اصلاحات Backend

### ۵.۱ اضافه کردن `index.ts` barrel exports به 18 `server/modules/`
- **تغییرات**: ایجاد فایل `index.ts` در هر زیرپوشه `server/modules/`
- **اولویت**: پایین

### ۵.۲ تغییر Prisma datasource از `sqlite` به `postgresql`
- **تغییرات**: تغییر `prisma/schema.prisma` و adapter
- **اولویت**: بالا (اگر قرار است production استفاده شود)

### ۵.۳ اضافه کردن JWT/RBAC به `src/middleware.ts`
- **تغییرات**: اضافه کردن اعتبارسنجی JWT و بررسی نقش در middleware
- **اولویت**: متوسط

---

## بخش ۶: اعتبارسنجی و تست

### ۶.۱ بررسی عملکرد جریان‌ها
- ثبت‌نام → انتظار تأیید → ورود پس از تأیید
- آپلود اکسل → استخراج شیفت → تقویم شخصی
- ثبت بخشنامه → تأیید اجباری → ذخیره receipt
- ثبت تیکت با تصویر → annotation → ارسال
- **اولویت**: بالا

---

## ترتیب اجرای پیشنهادی

1. **فاز A**: رفع خطاهای lint (1.1) + حذف console.log (1.3)
2. **فاز B**: جایگزینی رنگ‌های hardcoded (1.2) + اصلاحات RTL (3.1)
3. **فاز C**: نصب shadcn components_missing (4.1)
4. **فاز D**: اصلاحات ساختاری معماری (2.1, 2.2, 2.3)
5. **فاز E**: اصلاحات Backend (5.1, 5.2, 5.3)
6. **فاز F**: اعتبارسنجی جریان‌ها (6.1)

---

## بررسی نهایی

```bash
npm run build && npm run lint
```

**هدف**: 0 خطای lint، 0 رنگ hardcoded، build تمیز
