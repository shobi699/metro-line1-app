import { Platform } from 'react-native'

// آدرس آی‌پی سرور اصلی (VPS) برای اتصال اپلیکیشن موبایل
const SERVER_IP = '185.141.107.117'

export const API_URL = `http://${SERVER_IP}/api`
export const BASE_URL = `http://${SERVER_IP}`
